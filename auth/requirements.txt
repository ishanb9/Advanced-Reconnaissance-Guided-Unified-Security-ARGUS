# ARGUS auth-module dependencies live in the project root requirements.
#
# As of the documentation consolidation pass, everything ARGUS needs —
# core platform + auth + RAG + crypto — is in one unified file at:
#
#     ../requirements.txt
#
# Install with:
#     pip install -r requirements.txt
#
# Kept as a stub here so README links and bookmarks don't 404 after
# the consolidation.  Do NOT add packages here — add them to the root
# requirements.txt instead.
-r ../requirements.txt
