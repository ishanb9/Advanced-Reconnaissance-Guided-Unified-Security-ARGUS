# ARGUS auth-module Python dependencies.
# Add these to the top-level requirements.txt when integrating.

# ── Web framework (already in ARGUS — listed for transparency) ─
fastapi>=0.110.0
pydantic>=2.5,<3
email-validator>=2.1
python-multipart>=0.0.6

# ── ORM ────────────────────────────────────────────────────────
SQLAlchemy>=2.0,<3

# ── Cryptography ───────────────────────────────────────────────
argon2-cffi>=23.1
cryptography>=42.0
PyJWT[crypto]>=2.8

# ── MFA ────────────────────────────────────────────────────────
pyotp>=2.9
qrcode[pil]>=7.4

# ── Single sign-on ─────────────────────────────────────────────
# OIDC
authlib>=1.3
requests>=2.31           # for JWKS fetch (authlib doesn't pull this)
# SAML 2.0 — note: requires libxml2/libxmlsec1 system packages
python3-saml>=1.16
